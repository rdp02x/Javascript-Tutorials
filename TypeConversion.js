let name = "Rishabdev Panchal";
let age = 19;
let isStudent = true

// By default, the input type in JS is a string. 


let stringInput = 'Meoww'
console.log(typeof stringInput);

stringInput = Number(stringInput); // This is the number built-in function that converts any type of value to number type value

console.log(typeof stringInput);

// To display the type of a value we use 'typeof' before it.